const image = './images';
const icon = './icons';

export const IMAGES = {
  logo: require(`${image}/logo.png`),
  item_product: require(`${image}/item_product.png`),
};

export const ICONS = {
  cart_shopping: require(`${icon}/cart_shopping.png`),
};
